apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "craft-cms.fullname" . }}-test-connection"
  labels:
    {{- include "craft-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "craft-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
