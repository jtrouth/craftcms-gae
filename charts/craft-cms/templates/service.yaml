apiVersion: v1
kind: Service
metadata:
  name: {{ include "craft-cms.fullname" . }}
  labels:
    {{- include "craft-cms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "craft-cms.selectorLabels" . | nindent 4 }}
